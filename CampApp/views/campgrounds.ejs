<% include partials/header %>

<h1>This is the campgrounds page</h1>
<a href="/campgrounds/new">Add New Campground</a>
<% campgrounds.forEach(function(comground) { %>
    <div> 
        <h4> 
            <%=comground.name;%>
        </h4>
        <img src="<%=comground.image%>">
    </div>
    
<% }); %>

<% include partials/footer %>